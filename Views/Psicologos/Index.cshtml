@model IEnumerable<ConnectaMVC.Models.PsicologoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegistroAcademico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoPerfil)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegistroAcademico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPerfil)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PsicologoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PsicologoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PsicologoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
